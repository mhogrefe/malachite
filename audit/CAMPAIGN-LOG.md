# Documentation audit — campaign log (2026-08-03 to 2026-08-04, COMPLETE)

Historical record of the documentation audit: the plan, chunk-by-chunk status, findings log,
and retrospective. The durable output — conventions, cost cheat-sheet, and pitfalls checklist
for writing future documentation — lives in the repo-root **DOC-CONVENTIONS.md**; this file is
the evidence behind it.

The original campaign instructions follow, preserved as written. The goal was: every
documentation block in the four main crates verified against the code it describes, every
complexity claim carrying an argument for why it is accurate, and every scratch-buffer
requirement rederived from the implementation.

## Scale, measured 2026-08-03

Generated by `doc-audit-inventory.py` (repo root), which writes per-crate checklists to
`audit/checklist-<crate>.md`. Checked boxes survive regeneration; regenerate after refactors
that move items.

| crate           | items | complexity claims | scratch-takers | scratch-len formulas |
|-----------------|-------|-------------------|----------------|----------------------|
| malachite-base  | 1395  | 780               | 0              | 0                    |
| malachite-nz    | 2375  | 1911              | 108            | 61                   |
| malachite-q     | 408   | 372               | 0              | 0                    |
| malachite-float | 1476  | 1183              | 0              | 0                    |
| **total**       | 5654  | 4246              | 108            | 61                   |

Complexity claims include both the "Worst-case complexity" and (for random generators) the
"Expected complexity"/"Expected complexity per iteration" headers.

Two forms of leverage make this smaller than it looks:

- **Macro sharing.** Doc blocks generated by impl macros or doc macros
  (`apply_to_unsigneds!`-style families, `natural_mod_sqrt_doc!`-style shared docs) are audited
  once per macro, then every instance's checkbox may be ticked. Most of malachite-base is like
  this.
- **Delegation.** A wrapper whose body is a single call inherits its callee's complexity; once
  the callee is audited, the wrapper's audit is only "does the claim match the callee's, with
  variables renamed correctly?"

## What "audited" means for one item

Check the box only when all of the following have been verified against the implementation:

1. **Description and formula.** The prose says what the function does, including rounding
   directions, sign conventions, edge cases (zero, negatives, empty slices), and ownership
   phrasing. The $f(x, y) = \ldots$ LaTeX is correct.
2. **Panics.** Every claimed panic is reachable; every unconditional panic or assert in the
   function (or its unconditional callees) is claimed. Debug-only asserts are not documented.
3. **Complexity.** The $T$ and $M$ claims are correct, every variable appearing in them is
   defined in the `where` line, the definitions are the right ones (e.g. `n` is the bit length
   of the correct operand), and an accuracy argument is recorded per the convention below.
4. **Examples.** Doctests execute (so they are correct); check that they exercise the
   interesting cases the prose mentions.
5. **Scratch (items tagged `scratch`/`slen`).** See the next section.

Findings are fixed immediately, with the usual per-session verification (tests for behavioral
fixes, `rundoc.sh` for doctest changes, the lint sweep). Every finding, even ones fixed on the
spot, is recorded in the findings log at the bottom of this file: the pattern of what tends to
be wrong is itself a product of the audit.

## The complexity model (settle during the pilot, then write down once)

Many arguments become one-liners once the model is stated in a single place (proposed: a
`# Complexity conventions` section in each crate's top-level docs, written during the pilot):

- Word-RAM with `Limb`-sized words; $T$ counts limb operations, $M$ counts additional limbs.
- What $n$ means by default (`significant_bits` for bignum inputs, slice lengths for limbs
  functions), and that multiplication cost is written explicitly as
  $O(n \log n \log\log n)$.
- **The width-stability rule for primitive-int functions** (settled with Mikhail 2026-08-03,
  resolving the mod_sqrt-vs-mod_pow inconsistency in mod_pow's favor). Since primitive inputs
  are bounded, everything is technically $O(1)$; the honest rule is to analyze each function
  *as if generic over the width $W$* — which it literally is, being macro-generated over
  `u8`..`u128`:
  - Claim **"Constant time and additional memory"** only when the operation count is bounded
    by a constant independent of both $W$ and the input values: straight-line code, fixed
    trip-count loops, and single machine ops (add, mul, div, popcount, clz; `u128` ops count
    as $O(1)$ too, being a fixed number of native ops).
  - Otherwise state the real driver: $n = $ `x.significant_bits()` (or similar) when the trip
    count tracks an input's value, $W$ (the type's width) when it tracks the width regardless
    of value (e.g. a loop over all bits). Equivalently: the claim must stay correct if the
    same algorithm were instantiated at a larger width.
  - A one-time note in the conventions section says that for primitives these variables are
    bounded by $W$, so callers may read every such bound as a constant if they wish.
  - Test: primitive `mod_pow` is $T(n) = O(n)$, $n$ = exponent bits (correct today);
    primitive `mod_sqrt`'s "constant time" claim is a **finding** — Tonelli–Shanks is
    $O(n^2)$ modular ops in the worst case, $n$ = modulus bits, and the nonresidue search is
    value-dependent.

## Complexity-accuracy arguments

(Settled with Mikhail 2026-08-03: leaf-algorithms-only.)

- **Leaf algorithms** (an actual loop/recursion, not a delegation): one to three sentences in
  the doc block (public items) or internal comment (`limbs_*` items) stating the recurrence or
  dominant cost and why it solves to the claimed bound. Ported functions may cite the upstream
  source's analysis.
- **Delegators**: no argument; the audit only confirms the claim equals the callee's with
  correct variable translation.
- **"Constant time" claims**: no argument, but the audit confirms no hidden input-dependent
  loop (watch for value-dependent searches, as in the quadratic-nonresidue search).

## Scratch-requirement rederivation

The highest-value target: under-sized scratch formulas are a real, observed bug class (the
`limbs_mul_high_same_length_scratch_len` under-sizing found 2026-08-02 returned far less than
the Toom-band recursion consumed). For each of the 61 `*_scratch_len` functions and each of the
108 scratch-taking functions:

1. **Rederive** the peak scratch consumption from the implementation, including recursive
   calls, threshold-dependent branches (schoolbook/Toom/FFT bands!), and any slack the code
   assumes. Record the derivation as an internal comment next to the formula: what each summand
   pays for, and whether the result is exact or an upper bound (say which).
2. **Canary-verify the high-risk families** (settled with Mikhail 2026-08-03: canaries for
   the mul/div heartland only — `toom.rs`, `fft.rs`, `mul_low`/`mul_high`/`mul_mod`,
   `div_exact`, `div_mod`, `half_gcd`; simpler formulas rely on the derivation alone). Add or
   extend tests that fill the scratch with a sentinel, run the function across a size sweep
   straddling every algorithm threshold, and check the touched high-water mark against the
   formula (both directions: no overflow, and flag gross over-allocation). The 2026-08-02
   `mul_high_tests` module is the model.
3. Functions whose requirement is documented only in prose ("`scratch` must have length at
   least ...") get the same treatment; consider promoting recurring prose bounds to
   `*_scratch_len` functions where callers duplicate the arithmetic.

Threshold boundaries deserve special attention: the observed bug lived exactly in the band
between two algorithms, where the tested sizes never landed.

## Verification aids

- **Empirical scaling checks**: not part of the default bar (settled 2026-08-03); reserve
  for cases where a reading is genuinely inconclusive, and give the usual benchmarking
  heads-up first.
- **Mechanical well-formedness lint** (build during the pilot): every complexity block has
  both $T$ and $M$, a `where` line, and every variable used in the formulas is defined in it;
  every documented panic message matches an assert. This catches a whole class of errors
  cheaply and keeps the audited state from regressing. Candidate home: `additional-lints.sh`
  as a Python check, like `unincluded-files.py`.

## Known-suspect list (check early, they seed the findings log)

- FFT multiplication claims ($T$ and especially $M$) versus what `fft.rs` actually allocates.
- Ziv-loop float functions: worst-case versus expected time; whether claims account for the
  precision-doubling loop and for exponent-magnitude dependence, not just `prec`.
- Two-variable claims $T(n, m)$: whether `n` and `m` are attached to the right operands
  (several were written by analogy with neighbors).
- Division family: balanced vs unbalanced cases quoting the same bound.
- "Constant time" on width-bounded but value-dependent loops (see model note above).
- `M(n)` on in-place functions that allocate temporaries internally.

## Chunks, in dependency order (audit callees before callers)

0. **Pilot + conventions** (one session): write the complexity-conventions section; build
   the well-formedness lint and the scratch canary harness; audit one easy family end to
   end (nz `add`/`sub` limbs + wrappers) and one hard one
   (`mul` basecase + `limbs_mul_greater_to_out` + its scratch formula) to calibrate effort.
   **Checkpoint with Mikhail.**
1. **malachite-base** (1395 items): per-macro-family passes; mostly constant-time claims; the
   handful of real algorithms (primitive `mod_pow`, `gcd`, sqrt/root, `mod_sqrt`, primes,
   Kronecker, string/digit conversions) get leaf treatment.
2. **nz limbs kernels** (the heart, split into sub-chunks by family, each its own session or
   two, in this order): logic/comparison/shift → add/sub → mul (basecase, Toom family, FFT,
   `mul_low`/`mul_high`/`mul_mod` — the scratch heartland: 18 scratch-takers in `toom.rs`
   alone) → div/div_mod/div_exact/mod_op → sqrt/root → gcd/extended_gcd/half_gcd → modular ops
   (mod_mul/mod_pow) → digits/strings → factorization/primality.
3. **nz public wrappers**: Natural then Integer impl docs (mostly delegation checks plus
   prose/panics accuracy).
4. **malachite-q** (408 items): gcd-dominated claims; `Rational` reduction invariants in prose.
5. **malachite-float** (1476 items): wrappers first, then the Ziv-loop transcendental family
   with the special attention above.
6. **Wrap-up**: conventions doc finalized, well-formedness lint promoted into the sweep,
   findings-log retrospective.

## Tracking

- Per-item state: `audit/checklist-<crate>.md` checkboxes (regenerate with
  `python3 doc-audit-inventory.py`; checks are preserved by item name and file).
- Per-chunk state and findings: the tables below.

| chunk | status |
|-------|--------|
| 0 pilot + conventions | DONE 2026-08-03 (conventions in crate docs; well-formedness checker in additional-lints.sh; canary harness in test_util/scratch.rs; add/sub + mul-dispatcher audits complete, 62 items checked) |
| 1 base | DONE 2026-08-04: 1241/1241 (item counts shrank when the inventory stopped counting attribute-only "docs"); missing generator complexity sections written per the scope decision (110 new blocks) |
| 2 nz kernels | DONE 2026-08-04: 2321/2321 — all limbs kernels, twelve canaries, burn-down empty, orphaned-where-line net in checker |
| 3 nz wrappers | DONE 2026-08-04: completed together with the tail of chunk 2 (the wrapper files' claims are delegator-shaped and were verified family-by-family against the audited kernels); malachite-nz checklist is 100% checked |
| 4 q | DONE 2026-08-04: 387/387 — full-crate claim dump + mechanical nets, all anomalies triaged; six fix sites (see findings) |
| 5 float | DONE 2026-08-04: 1438/1438 — input-size-blind family fixed (~420 blocks), non-arithmetic half triaged, lib.rs TODOs filled |
| 6 wrap-up | DONE 2026-08-04: generator backfill (71 new blocks), retrospective below; **ALL 5,387 items checked; 4,468 complexity blocks well-formed** |

## Findings log

(append entries as `- file:item — what was wrong — how fixed`)

- 2026-08-03, pre-audit examples of the target bug classes, found incidentally: the
  `limbs_mul_high_same_length_scratch_len` under-sizing (scratch class); the width-bounded
  "constant time" inconsistency between primitive `mod_pow` and `mod_sqrt` (model class).
- 2026-08-03, format survey: five complexity blocks read literally `TODO` —
  `natural/arithmetic/sqrt.rs:645`, `natural/arithmetic/binomial_coefficient.rs:626,684`,
  `integer/arithmetic/binomial_coefficient.rs:28,112`. To fill in during the relevant chunks.
- 2026-08-03: `mod_sqrt` primitives claim "constant time"; under the width-stability rule
  Tonelli–Shanks is $T(n) = O(n^2)$ modular ops, $n$ = modulus bits. Fix during chunk 1.
- 2026-08-03, well-formedness sweep (`complexity-doc-check.py`, now in `additional-lints.sh`;
  all fixed same day): 8 `n$ is` missing-dollar typos in `natural/arithmetic/mod_op.rs`; six
  comparison impls (`integer/comparison/partial_{cmp,eq}_natural.rs`,
  `partial_cmp_primitive_float.rs`) with `where n = ...` lines missing the $T$/$M$
  definitions; `$n$ = ` instead of `$n$ is ` in geometric.rs (12), bools/random.rs (2),
  rounding_modes/from_str.rs, num/float/mod.rs, vecs/mod.rs; backticked `` `n` `` instead of
  `$n$` in striped.rs (11) and natural/random/mod.rs (5); missing `where` lines in
  factorial.rs (2), primorial.rs (2), shl.rs, prime_sieve.rs, integer/arithmetic/sub.rs;
  formula/declaration mismatches `$T(n) = O(b)$` in striped.rs (4) and `$T(n) = O(m)$` in
  strings/random.rs; `$T(n)$` claims using undeclared $m$ in vecs/mod.rs (2); one `$$`
  display-math outlier in vecs/exhaustive.rs; corrupted product LaTeX in primorial.rs
  (`=prod_{pleq natop ...}` — lost backslashes); "ee panics" typo in num/logic/traits.rs.
- 2026-08-03, pilot audit of nz add/sub (all fixed same day):
  - **min-vs-max worst-case class** (the pilot's biggest find): the by-value `Add`/`AddAssign`
    for `Natural`, `Add`/`AddAssign`/`Sub` for `Integer`, `ModPowerOf2Add`, and the limbs
    kernels `limbs_slice_add_in_place_either`/`limbs_vec_add_in_place_either` all claimed $n$ =
    `min(...)` of the operand sizes, but a carry/borrow propagates through the entire longer
    operand ($x = 2^k - 1$, $y = 1$), making the worst case `max`. The `min` claims on
    comparisons (`partial_cmp`/`partial_eq`) and on `And`/`Or`/`Xor` are CORRECT — those have
    no carry chain (and truncates; or/xor leave the longer operand's high limbs alone).
  - All four `Natural` `Sub` impls panic when `other > self` but had no `# Panics` section
    (only the `SubAssign` impls documented it).
  - `limbs_sub_limb_in_place` cited `mpn_add_1` as provenance instead of `mpn_sub_1`.
  - Growth-only memory accounting: `limbs_vec_add_in_place_left` and
    `limbs_vec_sub_in_place_right` claimed $M(m) = O(m)$ counting only the `Vec` growth, but
    reallocation copies the whole result — normalized to the siblings' $M(n) = O(n)$
    (reallocation parenthetical).
  - `limbs_add_to_out_aliased` claimed $n$ = `max(xs.len(), ys.len())` but only touches
    `xs[..ys.len()]` — tightened to `ys.len()`.
  - Typos: "Interpreting a two slices" (4×), "Interpreting a of `Limb`s",
    "`xs.len()` is shorter than `ys.len()`".
- 2026-08-03, pilot audit of the mul dispatchers + scratch formulas (fixed same day):
  - `limbs_mul_greater_to_out_basecase`'s doc said "Returns the result limb at index ..." but
    the function returns nothing (copy-paste from the returning variants); bound tightened to
    the exact $T(n, m) = O(nm)$ with a one-line argument.
  - Neither mul dispatcher documented its scratch-length requirement; the same-length one has a
    defensive assert that panics on short scratch (≥ Toom-33 sizes), which was an undocumented
    panic. Both now state the `*_scratch_len` contract.
  - Both `*_scratch_len` formulas verified branch-by-branch against their dispatchers
    (conditions match exactly, including the unbalanced chunking reduction loops); derivation
    comments added stating the lockstep invariant.
  - Canary tests added (`test_limbs_mul_*_scratch_sizing` in tests/natural/arithmetic/mul.rs,
    harness in `test_util/scratch.rs`): exact-formula scratch across all threshold boundaries
    plus ratio sweeps; both pass.
- 2026-08-03, code finding, FIXED same day (user-approved): for `xs.len() != ys.len()` below
  the FFT band, `limbs_mul_greater_to_out` ignored the caller-provided scratch entirely and
  allocated its own internally, so callers paid for a buffer that was never read. Now the
  caller's scratch is passed through to `_old`, with a clear-message defensive length assert
  (mirroring the same-length dispatcher's); all 16 calling files size their scratch via the
  `*_scratch_len` formulas, and any latent shape mismatch now fails loudly instead of being
  masked by the double allocation.
- 2026-08-03, suspects logged for later chunks: `malachite-float/src/lib.rs` crate docs have
  two TODOs (demo/bench commands never filled in) — fix in chunk 5. $M(n) = O(n \log n)$ on
  the mul dispatchers looks loose (Toom scratch totals are $O(n)$; check what the FFT
  actually allocates) — tighten in chunk 2's mul pass.
- 2026-08-03, chunk 1 (base), first pass over the arithmetic/factorization leaf algorithms
  (all fixed same day):
  - **Bignum-claims-pasted-onto-words class** (the chunk's dominant find): `gcd`,
    `extended_gcd`, `mod_inverse`, `lcm`/`checked_lcm`, `coprime_with`, and all six
    Legendre/Jacobi/Kronecker impls claimed $T(n) = O(n^2)$, $M(n) = O(n)$ — the *bit*
    complexities of the bignum versions. On words these algorithms are $O(n)$ time (each
    iteration removes at least one bit) and allocation-free, so $M = O(1)$. All fixed with
    leaf arguments.
  - `mod_power_of_2_inverse` claimed $T(n) = O(n)$, but Hensel lifting doubles the solved
    precision per step: tightened to $O(\log n)$.
  - `Factor` for u32/u64/u128/usize claimed $M(n) = O(2^n)$ — absurd; the SQUFOF working
    state is all fixed-size arrays, so $M = O(1)$. $T = O(2^{n/4})$ kept, marked heuristic.
  - `mod_sqrt` "constant time" fixed to $T(n) = O(n^2)$ under the width-stability rule
    (Tonelli–Shanks), mirroring the nz claim's ERH caveat; derivation comment on the leaf.
  - Width-rule violations found by the loop-triage script: `Seed::fork` (hashes the whole
    key, $O(n)$ in `key.len()`), `VariableRangeGenerator::next_bit_chunk` ($O(n)$ in
    `chunk_size`), and `next_less_than`/`next_in_range`/`next_in_inclusive_range`
    (rejection sampling: converted to "# Expected complexity" with the unbounded-worst-case
    note). `increment_counter` and `from_other_type_slice` were flagged but are legitimately
    constant (fixed trip counts).
  - New header variant discovered: `# Worst-case complexity (amortized)` (11 blocks,
    primes.rs and elsewhere) — added to `complexity-doc-check.py`'s header set; all pass.
    The two sieve `jump_after` memory claims ($O(1)$ vs $O(n)$) look inconsistent but are
    both correct (fixed sieve scan vs sieve regrowth).
  - Leaf arguments added to the correct-but-unargued claims: `mod_pow` family (ladder),
    `pow`/`overflowing_pow`, `log_base`, `is_prime` (bounded Miller-Rabin rounds),
    `primitive_root_prime` (factoring $p - 1$ dominates).
  - Known triage limitation: the loop-scan can't see through delegators (a constant claim on
    a thin wrapper whose callee loops), so delegator claims are checked against their
    audited callees per family instead.
- 2026-08-04, chunk 1 second pass (conversions/logic/slices/strings/foer_sequences/iterators
  + a mechanical **delegator check**: constant-claimed bodies calling known non-constant
  functions — 59 flags, most false positives, all triaged). Fixed same day:
  - **128-bit width class**: macro-uniform "constant time" claims covered u128/i128 impls
    that delegate to the $O(n)$ binary-search square root: the signed sqrt macro, the sqrt
    assign macros, the u128 root impls, all three root macros (16 blocks), and u128
    `is_square` (whose residue prefilters are constant but survivors pay a 128-bit sqrt).
    All now claim $O(n)$ with a constant-below-128-bits note.
  - `gmp_format` for primitives and `char` claimed constant but pads to the requested field
    width/precision: now $O(n + w + p)$.
  - `FoerSequence::cmp` claimed $O(n)$ in `self` alone, but after the equality pretest two
    distinct reduced sequences can first differ as late as the max prefix plus
    $\mathrm{lcm}$ of the period lengths: now $T(n, m) = O(nm)$ with the argument.
    `FoerSequence::mutate` claimed $O(index)$ but re-reduces, paying the minimal-period scan
    on the whole repeating part: now $O(n + m^{1+\varepsilon})$.
  - `string_is_subset` claimed $T(n) = O(n)$ but builds a hash set of the second string:
    now $T(n, m) = O(n + m)$, $M = O(m)$. `min_repeating_len` claimed $M(n) = O(n)$ but
    allocates nothing: now $O(1)$; its description also had the roles of $\ell$ and $n$
    reversed.
  - Verified clean: digits/string conversions, slice conversions, bit_convertible,
    comparison/logic traits, bit_distributor (fixed 64-bit counter ⇒ the flagged constant
    claims are legitimate), iterator_cache, primes/prime_sieve (sieve claims standard),
    float `pow` (libm, constant), fork-callers with fixed keys.
- 2026-08-04, chunk 2 mul family (all fixed same day):
  - toom.rs: derivation comments added to all 13 scratch formulas (split-mirroring + lockstep
    invariant + canary citation); the 18 kernel complexity blocks got the argument that point
    multiplications re-enter the main dispatcher (which is why $O(n \log n \log\log n)$ is
    correct rather than the standalone Toom exponents); the 26 constant claims on
    scratch_len/validity functions hold because the dispatcher formulas' shape-reduction loops
    run $O(1)$ iterations within valid Toom shapes.
  - Suspects-list item RESOLVED: the FFT holds all CRT primes' coefficient data simultaneously
    and the prime count grows as $O(\log n)$, so the mul dispatchers' $M(n) = O(n \log n)$ is
    the honest asymptotic — claims stand.
  - mul_low.rs: both divide-and-conquer variants claimed $T(n) = O(n^{\log_8 15})$ (an
    exponent that assumed Toom-8h multiplication) with $M(n) = O(1)$ despite allocating their
    multiplication scratch internally — now $O(n \log n \log\log n)$ / $O(n \log n)$ with
    the geometric-shrink argument; the two basecases claimed $M(n) = O(n)$ but allocate
    nothing — now $O(1)$; the dispatcher's dangling "assuming $k = O(\log n)$" clause
    (with $k$ undefined) removed.
  - fft.rs (76 items) and context.rs carry no complexity blocks, consistent with the
    limbs-internal minimal-docs convention (newer FLINT port; comments + provenance only);
    poly_eval/poly_interpolate linear claims verified.
- 2026-08-04, chunk 2 div family: nine scratch canaries landed across
  div/div_mod/div_exact/mod_op tests — threshold-dense sweeps (12 division thresholds via a new
  `div_dispatch_thresholds()` test_util helper) with exactly-sized sentinel scratch and full
  value oracles: exact quotient/remainder against `Natural` for the Barrett q/qr/r forms,
  error-window checks for the approximate forms (quotient within +4; Newton inverse within 2
  ulps of $\lfloor (B^{2d} - 1)/D \rfloor - B^d$), and $QD \equiv N \pmod{B^k}$ for the
  2-adic forms. All pass; no under-sizing anywhere in the swept shape spaces. Derivation
  comments added to all 13 div-family scratch formulas (GMP itch-function ports; buffer-carving
  structure; canary citations). Claim survey: distributions internally consistent (the standard
  reduce-to-multiplication claims; tight schoolbook $O(d(n - d + 1))$). Two loose-but-valid
  $M(n) = O(n(\log n)^2)$ claims on mod_op DC helpers noted (siblings say $O(n \log n)$) —
  candidates for tightening, not errors.
- 2026-08-04, tightening pass (user-requested):
  - mod_op's two DC-helper memory claims $O(n(\log n)^2) \to O(n \log n)$ with the argument
    (each recursion level frees its multiplication scratch before recursing, so the peak is a
    single level's multiplication memory), and the chunking helper's $O(d(\log d)^2) \to
    O(d \log d)$.
  - geometric.rs: all 12 Expected-complexity bounds tightened from $n$ = `numerator +
    denominator` to the exact expected trial count, $n$ = `numerator / denominator + 1`
    (the coin's continue-probability makes expected trials exactly $(num + den)/den$).
  - Float $(\log n)^2$ memory family: INVESTIGATED AND RESOLVED same day (user-directed).
    Read every core machine: AGM (`agm_prec_round_normal`) holds a bounded set of
    working-precision values per iteration; Brent-Salamin pi likewise (the $c_k$ sum is
    accumulated, not stored); ln composes AGM + pi + ln_2; ln_2 is binary splitting whose
    3-lg-N Integer stack has geometrically growing entries totaling $O(n)$ limbs; exp
    dispatches the Paterson-Stockmeyer power table (super-linear memory) only BELOW the exp
    threshold, using binary-splitting `exp_3` asymptotically. Every path peaks at the internal
    memory of one full-precision multiplication, $O(n \log n)$ — and agm.rs and exp.rs
    already claimed exactly that, confirming the intended convention. The $(\log n)^2$ was
    the depth-times-level over-count written by analogy with the (genuine) $(\log n)^2$ in
    the time bounds. **329 claims tightened across 39 float files**; the constant-working-set
    argument recorded on the pi core.
- 2026-08-04, chunk 2 square family: canary landed (dispatcher sweep to 1100 limbs plus all
  five Toom squaring kernels over every valid length via their `input_size_valid` predicates);
  derivation comments on all six squaring scratch formulas. Key structural finding: unlike the
  mul Toom kernels, the squaring recursions select ONLY within the Toom-square family (never
  the FFT), so the standalone exponent claims ($n^{\log_2 3}$ through $n^{\log_8 15}$) and
  the $M(n) = O(1)$ claims are all CORRECT — the opposite verdict from mul's kernels, for a
  checkable structural reason. One defect fixed: both toom_8 blocks wrote toom_6's formula
  $n^{\log_6 11}$ next to toom_8's correct approximation 1.302 ($= \log_8 15$) — the
  formula, not the approximation, was the copy-paste error.
- 2026-08-04, chunk 2 sqrt/root + gcd families:
  - sqrt.rs's burn-down TODO resolved: `limbs_ceiling_sqrt` now claims the sibling floor form
    (one sqrt-with-remainder, a linear zero-test, at most one increment); burn-down list is
    down to 4 (the binomial_coefficient blocks). Derivation comment on
    `limbs_sqrt_rem_helper_scratch_len` (half the input plus a guard limb, per
    `mpn_dc_sqrtrem`). root.rs's $(\log n)^2$ time claims verified (Newton iterations times a
    multiplication each; the root index's contribution is width-bounded).
  - Twelfth canary: `test_limbs_gcd_reduced_scratch_sizing` drives the whole half-GCD stack
    (Lehmer steps, divide-and-conquer, FFT-based reduction) across all four GCD thresholds to
    1750 limbs with the `Natural` gcd oracle; two file-private thresholds promoted to
    pub(crate) for the sweep helper. Derivation comments on all three hgcd scratch formulas
    (subdivide quotient buffer; four half-size-plus-guard matrix elements; the `mpn_hgcd_itch`
    recursion bound). gcd/mod.rs, extended_gcd.rs, matrix_2_2.rs claims uniform and verified.
  - Design question answered (user): the Toom squaring kernels' self-contained recursion never
    needs the FFT because kernels are only entered within their dispatcher bands, where the
    point squarings sit far below the FFT threshold; the recursive selectors coincide with
    what the dispatcher would choose. Direct out-of-band kernel calls (tests only) get the
    documented standalone exponents.
- 2026-08-04, chunk 2 modular + digits + fused batch:
  - Modular families verified (271 items): mod_mul/mod_pow claims uniform with the
    precomputed-work docs; the three $O(n^2)$ blocks are the schoolbook REDC kernels (n addmul
    passes, allocation-free) — correct. eq_mod, mod_inverse, mod_power_of_2 families all
    consistent.
  - general_digits (61 items): the striking $T(n) = O(2^n n \log n)$ power-table claims are
    EXACTLY the standard multiplication bound reparameterized by table depth ($N = 2^n$ limbs
    $\Rightarrow N \log N \log\log N = 2^n n \log n$) — verified correct, including the
    memory. DC digit-conversion claims standard.
  - **All four remaining burn-down TODOs resolved**: Natural and Integer binomial_coefficient
    now claim $T(n, k) = O(nk (\log(nk))^2 \log\log(nk))$ (raising-factorial product tree +
    exact division, within polylog of the $O(nk)$-bit result). Two corrupted formulas fixed
    (`=binom`/`=frac` lost backslashes, same class as primorial's), and the undocumented
    "k is too large" limb-overflow panic documented on both Natural impls. The
    KNOWN_PLACEHOLDERS burn-down list in complexity-doc-check.py is now EMPTY — every one of
    the 4,395 complexity blocks in the workspace is well-formed with no placeholders.
  - Fused-mul files (add_mul/sub_mul/mul_add_mul/mul_sub_mul + saturating/checked variants,
    167 items with kronecker/pow/binomial): claims fresh and correct from the 2026-07-29 perf
    rewrite ($O(m + n \log n \log\log n)$ shapes); nz kronecker correctly carries the
    hgcd-based subquadratic bound, distinct from base's word-level $O(n)$.
- 2026-08-04, chunk 1 third pass (algorithm stragglers + generator families + constant mass):
  - **Private-helper delegator class** (closed with a third mechanical net — same-file loopy
    helper scan): `is_power` (16 blocks) claimed constant but its helpers trial-divide
    (each division removes a bit) and take up to ~31 roots → $O(n)$ with argument;
    `remove_power` (2 blocks) claimed constant but loops dividing out the factor → $O(n)$.
    fibonacci/factorial/primorial constants are legitimate (table lookups).
  - The same-file scan's other 5 flags are bounded and legitimate (fixed-size factor arrays,
    width-ratio conversion loops, Ryu's digit cap, ChaCha block refills).
  - Two more header variants found and added to the checker: `# Worst-case complexity per
    iteration` (58 blocks, previously unscanned). That scan flushed: 5 blocks in
    malachite-q's rational/exhaustive with $T(i)$ formulas but `$n$ is the iteration
    number` where-lines (wrong-variable class), one mixed $T(n)$/$M(i)$ block, and a
    foer_sequences block whose $i$ was never defined.
  - Generator-content spot checks: geometric's $n$ = numerator + denominator is a valid
    upper bound on the mean-proportional expected work; striped's mean-length and
    length-range claims verified.
  - Constant-time mass (~750 items) checked off on the strength of three mechanical nets
    (own-loop scan, same-file loopy-helper scan, known-non-constant callee scan — every flag
    triaged) plus per-family samples read across the three passes.
- 2026-08-04, scope decision RESOLVED (user chose: write the missing blocks): 110 new
  complexity sections added to the block-less generator modules — num/exhaustive (32,
  all constant per iteration), sets/exhaustive (28, $\ell$-parameterized, hash = expected /
  b-tree = worst-case with the $\log$), vecs/random (18) and sets/random (12)
  (mean/range/length-iterator-parameterized Expected claims, with the distinct-values
  termination caveat on the unique/set variants), strings/exhaustive (8), tuples/exhaustive
  dependent-pairs family (5, callee-relative $T^\prime$/$T^{\prime\prime}$ form), options
  (4), foer_sequences/random (1). Also fixed the inventory to stop counting attribute-only
  "docs" as documented items (5,654 → 5,387 real items repo-wide).
  NOTE for later chunks: vecs/exhaustive and a few similar files were checked off earlier
  despite lacking complexity sections on most items; now that the convention is
  "generators carry per-iteration blocks", a follow-up sweep should backfill those too
  (mechanical detector: documented pub fn returning an iterator with no complexity header).
- 2026-08-04, chunk 2 integer logic kernels (and/or/xor, 71 items; all fixed same day):
  - Two M-claims in `integer/logic/and.rs` said $O(1)$ on functions that allocate $O(n)$:
    `limbs_and_pos_neg` (returns a fresh `Vec`) and `limbs_vec_and_pos_neg_in_place_right`
    (extends the `Vec`); their siblings claimed $O(n)$ correctly.
  - `integer/logic/or.rs`: `limbs_vec_or_pos_neg_in_place_left`'s `x_i >= ys_len` branch did
    `*xs = ys.to_vec()` — an $O(n)$ allocation contradicting the claimed
    $M = O(\max(1, m_y - m_x))$ — CODE FIX to the sibling's allocation-free
    `truncate` + `copy_from_slice` idiom. `limbs_slice_or_neg_neg_in_place_left`'s prose said
    leftover high limbs are set to `Limb::MAX`; the code zeroes them.
  - `integer/logic/xor.rs`: `limbs_xor_pos_neg_in_place_right` claimed growth
    `max(1, ys.len() - xs.len())` but the receiving `Vec` is `ys`, which grows by
    `max(1, xs.len() - ys.len())` — wrong-direction copy-paste from the `_left` twin (new
    bug class: **mirrored-variant claims not mirrored**). `BitXor<&Integer> for &Integer` had
    lost its entire complexity block (header + formulas), leaving an orphaned `where` line —
    invisible to the header-anchored checker. By-value `BitXorAssign`'s neg-pos arm delegated
    to the by-ref path, growing a `Vec` by $O(n)$ and falsifying the documented $M = O(1)$ —
    CODE FIX: route through the by-value pos-neg path via `mem::take` (uses
    `in_place_either`, true $O(1)$, matching how the and/or by-value arms achieve it).
    Also two duplicated-phrase stutters and a "bitwise and" in a xor description.
  - `natural/logic/xor.rs`: the `BitXorAssign<&Natural>` doctest used `|=` with by-value
    operands — it compiled, passed (disjoint masks: or == xor), and demonstrated the wrong
    operator and the wrong impl. Fixed to `^= &`.
  - New mechanical net from the lost-header find, now permanent in
    `complexity-doc-check.py`: a `where $T$ is time` line with no complexity header above it.
    Swept all 4 crates: 6 hits — the xor block above; `natural/arithmetic/div.rs`
    `limbs_div_barrett_approx_preinverted` (header line missing, and `$M(n)$` declared where
    the bound is in `ds.len()` — now `$M(d)$`); and 4 in `strings/exhaustive.rs`, which used
    a sixth, unrecognized header variant (`# Complexity per iteration`) plus stacked
    duplicate blocks from the chunk-1 backfill — normalized to one well-formed block per
    generator (and `exhaustive_fixed_length_strings` gained its missing block).
  - `natural/random/mod.rs` tightened: 6 mean-precision claims $T(n, m) = O(n + m)$ →
    $O(n / m + 1)$ (the mean, matching the already-tight $M = O(n / m)$), and 2 `mean_bits`
    sum-form definitions → quotient form, mirroring the chunk-1 geometric tightening.
- 2026-08-04, chunk 2 integer bit-access/conversion batch (all fixed same day):
  - `integer/logic/bit_access.rs`: the neg-side kernels claimed $T = O($`index`$)$, but
    setting/clearing a two's complement bit of a negative number adds or subtracts a power of
    2 from the stored magnitude, whose carry/borrow (and the leading-zeros scan) can run
    through the ENTIRE slice — `xs = [0, ..., 0, 1]` with `index = 0` costs $O(n)$. Three
    kernel claims re-based on `xs.len()` (or `max(xs.len(), index / Limb::WIDTH)` for the
    `Vec`-growing one) and both `Integer::set_bit`/`clear_bit` wrapper claims re-based on
    `max(self.significant_bits(), index)`, all with carry-chain derivation notes. The
    Natural-side $O($`index`$)$ claims are genuinely correct (resize + one limb write, no
    chain) — this is the min-vs-max class wearing a new coat: **index-vs-length**.
  - `integer/logic/bit_block_access.rs`: a garbled self-referential where-line
    (`$n$ is max(n / 2 ^ Limb::WIDTH, m)`) rewritten; two claims said
    `max(xs.len(), end * Limb::WIDTH)` — wrong units (bit index times limb width) — now
    `end / Limb::WIDTH`; `Integer::assign_bits` claimed $M = O($`self.significant_bits()`$)$
    but assigning bits beyond the current width grows the number to `end` bits.
  - `integer/conversion/natural_from_integer.rs`: `SaturatingFrom<&Integer>` claimed constant
    time but clones the absolute value — now $O(n)$; four doc lines said "taking the
    [`Natural`] by value/reference" where the thing taken is the Integer.
  - Everything else in integer logic/conversions verified clean (hamming's `xs.len()` claims
    are guarded by an equal-length precondition; `NLIterator` per-item costs are genuinely
    amortized-constant; pyo3 stays blockless per the FFI-glue convention).
- 2026-08-04, chunks 2+3 wrap-up batch (natural + integer wrappers, random, exhaustive,
  factorization; all fixed same day):
  - `natural/exhaustive/mod.rs` (5 blocks) and `integer/exhaustive/mod.rs` (13 blocks): the
    range/exhaustive iterators claimed $T(i) = M(i) = O(i)$ per iteration — a gross
    over-claim; every `next` clones the current value ($\Theta(\ell)$, $\ell$ = output's
    significant bits) and increments (amortized constant). All rewritten in the
    $\ell$-parameterized form, and the accompanying "worst case is very rare ... otherwise
    constant" paragraphs (which were wrong: the clone is never constant, and contained
    "the the" typos) removed. Integer's five interleaving generators also claimed
    "$M(i) = O(1)$, amortized." — each item is a fresh allocation.
  - `natural/arithmetic/fibonacci.rs` (4 blocks): $M(n) = O(n)$ under-claimed — the doubling
    steps call `limbs_mul_greater_to_out` with its full scratch, $O(n \log n)$ by the house
    mul-memory convention.
  - `natural/arithmetic/primorial.rs`: both fns under-claimed $T$ by one $\log$ factor
    (`limbs_product`'s tree costs $M(N) \log N$; unlike factorial's prime-swing recursion,
    the flat product tree is not geometric). `primorial` → $O(n (\log n)^2 \log\log n)$;
    `product_of_first_n_primes` (result $\Theta(n \log n)$ bits) →
    $O(n (\log n)^3 \log\log n)$ with $M = O(n (\log n)^2)$. Verified `factorial`'s
    $(\log n)^2$ and `subfactorial`'s $(\log n)^3$ are correct as claimed.
  - `integer/random/mod.rs`: same tightening as natural's — 18 mean-precision claims
    $O(n + m) \to O(n / m + 1)$ and 4 sum-form `mean_bits` definitions → quotient form.
  - Verified as correct along the way: `Integer` `Sum`'s $T = O(n^2)$ is genuinely tight
    (sign oscillation around $2^k$ forces a full carry chain per addition — impossible for
    monotone Natural sums); `Product`'s $(\log n)^2$ matches `limbs_product`; eq_mod's
    constant-time single-limb congruence (the documented $k = 1$ argument); comparison
    min-claims (no carry chains); shr's `max(1, ...)` forms; integer factorization mirrors.
  - One typo: a stray `.s` after a where-line in
    `natural/arithmetic/round_to_multiple_of_power_of_2.rs`.
  With this batch the malachite-nz checklist is fully checked (2,321 items): chunks 2 and 3
  are both DONE. Next: chunk 4 (malachite-q).
- 2026-08-04, chunk 4 (malachite-q, 387 items; all fixed same day):
  - `conversion/from_numerator_and_denominator.rs`: FIVE blocks claimed $T = O(n^2)$,
    $M = O(n)$ on the word-sized constructors (`const_from_unsigneds` & co.) — the chunk-1
    "bignum claims pasted onto word algorithms" class again; the unrolled word Euclid gcd is
    $O(n)$ word operations with no allocation. Now $O(n)$/$O(1)$ with the width-stability
    note matching base's gcd docs.
  - `comparison/partial_eq_primitive_int.rs`: four $T = O(n)$ claims on comparisons that are
    two constant-time checks (`denominator == 1 && numerator == x`) — the structurally
    identical `eq_abs_primitive_int` already said constant. Now constant.
  - `conversion/string/from_sci_string.rs`: both `from_sci_string_simplest` variants
    under-claimed — on top of parsing (bit bound $B = 10^n \log m$), they run
    `simplest_rational_in_closed_interval`, a continued-fraction expansion quadratic in $B$;
    the plain variant even claimed $O(10^n n \log n)$, missing the square entirely. Both now
    $T = O(B^2 \log B \log\log B)$ with the two-stage derivation note.
  - `arithmetic/mul.rs`: `MulAssign<&Self>` claimed $(\log n)^3$ — copy-pasted from the
    `Product` impl below it; the binary cross-gcd algorithm is $(\log n)^2$ like its five
    siblings. (`Product`/`Sum`'s $(\log n)^3$ are correct: a gcd at every merge level of the
    tree.)
  - `conversion/digits/from_digits.rs`: `\log (nm)^2` bracketing → `(\log (nm))^2` (2
    blocks); `exhaustive/mod.rs`: one block used $n$ for the iteration number where all its
    siblings use $i$.
  - Verified clean: the exhaustive iterators' $\log i$-parameterized claims (including the
    $(\log\log i)^2$ gcd-of-log-size forms — no O(i) class here, unlike nz); random's
    already-quotient-form mean claims; the continued-fraction quadratic family
    (approximate, simplest_rational_in_interval, express_as_power, from_float_simplest);
    cross-multiplication comparison claims; `Rational::significant_bits` constant.
  Chunk 4 DONE. Next: chunk 5 (malachite-float, 1438 items, Ziv-loop scrutiny).
- 2026-08-04, chunk 5 (malachite-float) part 1 — THE BIG ONE, **input-size-blind
  transcendental claims**: 334 blocks across 29 arithmetic files claimed cost in `prec`
  alone. But an input's precision can dwarf `prec`, and every such function at least scans
  its input (truncation with correct `Ordering`, argument-reduction subtractions — ops whose
  own claims DO carry operand bits), while rational-input variants can run rational
  arithmetic (gcd class) or exact-root detection (mul class). User decision: **additive
  per-family m-terms**, $T(n, m) = O(f(n) + g(m))$. Applied per family after reading the
  rational helpers:
  - linear $+ m$: add/sub_prec (sticky-scan), exp/ln/agm/sqrt-float-side, ln_rational and
    agm_rational (pure from_rational squeeze), log_base_2/log_base_power_of_2 (power-of-2
    detection is linear);
  - mul class $+ m \log m \log\log m$: sqrt/reciprocal_sqrt/root rational (checked_sqrt /
    checked_root exact detection), all general-base log families (exact-power-of-base
    detection), pow_rational-side;
  - gcd class $+ m (\log m)^2 \log\log m$: exp/exp_x_minus_1/power_of_2/power_of_10 (and
    x_minus_1) rational variants — their near-one paths do Rational multiplication/Taylor
    (e.g. `power_of_2_rational_near_one` computes `x * ln_2_lo` as a Rational; expm1's
    sub-MIN half_gcd branch).
  - Word-exponent pow (pow_u/pow_s/unsigned_pow): $T(n, m) = O(mn \log n \log\log n)$, m
    = exponent bits, n = max(prec, self bits) — mirroring the nz Natural::pow convention.
  - no-prec `_round` forms referencing a nonexistent `prec` re-based on
    `self.significant_bits()` / "the precision of the input" (+ base m-terms for
    float/rational-base log variants). Word-input fns (ln_unsigned, sqrt_unsigned,
    power_of_2_prec) keep bare prec — correct, inputs bounded.
  Total ~370 blocks rewritten; plus 21 `primitive_float_*_rational` emulation wrappers that
  claimed CONSTANT with unbounded Rational inputs (now the family m-term with prec = O(1)).
  - Also fixed, same class: `Float::get_min_prec` and `to_significand` claimed constant
    (trailing-zeros scan / significand clone are $O(n)$); both `from_raw_mantissa_and_exponent`
    blocks (trailing-zeros scan); ALL 15 blocks in `primitive_int_from_float.rs` +
    `primitive_float_from_float.rs` claimed constant, but `shr_round` by ~n bits must examine
    every discarded bit for the rounding/Ordering; `$M(n,)$` trailing-comma typo in
    `random/mod.rs`. (`into_significand`/`significand_ref` genuinely are constant — move and
    borrow.)
  Remaining for chunk 5: conversions/string/random/exhaustive/constants/comparisons triage
  and checkoff, lib.rs crate-doc TODOs, format_float padding scrutiny.
- 2026-08-04, chunk 5 part 2 + chunk 6 wrap-up:
  - Float non-arithmetic half: the six re-precision fns (`set_prec`, `set_prec_round`, the
    four `from_float_prec` forms) claimed $n$ = `prec` only — shrinking must scan the
    discarded bits (the same input-blind class; now $O(n + m)$).
    `from_rational_prec_round` was the odd sibling out: linear $T$ where its three siblings
    say $O(n \log n \log\log n)$, AND its where-line referenced `n.significant_bits()` — a
    parameter it doesn't have (copy-paste from `from_natural`). Float random got the
    mean-quotient tightening (14 blocks). `format_float` already carries the padding terms
    ($p$, $w$) — the gmp_format lesson was applied there. from_bits/from_digits prec-only
    claims are CORRECT (iterator-fed, lazily consumed). The two crate-doc TODOs in
    `lib.rs` (demo/bench instructions) filled with the nz-style walkthrough.
  - Chunk-1 backfill debt cleared: 71 new per-iteration blocks on the generator functions
    that were checked off before the convention existed — 39 in `vecs/exhaustive` (the
    callee-relative $\ell$-form mirroring sets/exhaustive), plus 32 across
    num/random, sets/exhaustive+random (hash vs b-tree $\log \ell$ split), vecs/random,
    strings, tuples, chars, striped, iterators (incl. `nonzero_values`' skip-count $z$ and
    the unique-pairs distinct-values caveat).

## Retrospective

Campaign ran 2026-08-03 to 2026-08-04. All 5,387 checklist items checked; 4,468 complexity
blocks well-formed under the checker; 12 scratch canaries with value oracles guard the
mul/div/square/gcd heartland; ~1,100 complexity blocks corrected or written.

**Bug-class taxonomy, roughly in order of discovery:**
1. min-vs-max worst case (pilot: by-value add/sub carry chains)
2. bignum claims pasted onto word algorithms (base gcd/xgcd/jacobi; recurred in q's
   const-constructors)
3. 128-bit width class (macro-uniform "constant" over O(n) delegators)
4. padding-blindness (gmp_format) and lcm-blindness (FoerSequence::cmp)
5. T-analogy memory claims (float $(\log n)^2$ M copied from genuine $(\log n)^2$ T)
6. index-vs-length carry chains (Integer bit_access neg side)
7. mirrored-variant claims not mirrored (xor in_place_right growth direction)
8. lost-header blocks (invisible to the checker; now a permanent net)
9. exhaustive-iterator per-iteration over-claims (clone is $\Theta(\ell)$, not $O(i)$ or
   amortized-constant)
10. product-tree log factor (primorial vs factorial's geometric prime-swing)
11. **input-size-blind precision claims** (the float flagship: ~420 blocks costed in `prec`
    alone; fixed additively with per-family m-terms — linear scan / mul-class detection /
    gcd-class rational arithmetic)

**Permanent infrastructure left behind:** `complexity-doc-check.py` in `additional-lints.sh`
(well-formedness + orphaned-where-line net over every block, all four crates);
`doc-audit-inventory.py` + `audit/` checklists (regenerable, checks keyed by (file, item));
`test_util/scratch.rs` sentinel-canary harness + threshold-straddling sweeps; the
width-stability rule and complexity conventions in the malachite-base crate docs;
the per-iteration generator convention (callee-relative $\ell$-form).
